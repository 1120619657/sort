# SORT 阶段一最终缺陷记录

## P1-BUG-001 空 tracker 分支返回错误形状

- 复现：0 detection + 0 tracker。
- 预期：`unmatched_trackers.shape == (0,)`。
- 原始实际：`(0,5)`。
- 修复：改为空的一维整数索引数组。
- 状态：已修复并回归通过。

## P1-BUG-002 空代价矩阵返回格式不稳定

- 复现：`linear_assignment(np.empty((0,0)))`。
- 预期：空结果保持两列配对结构 `(0,2)`。
- 原始实际：`(0,)`。
- 修复：入口显式返回 `(0,2)` 整数数组。
- 状态：已修复并回归通过。

## P1-BUG-003 单行检测文件导致 demo 读取维度异常

- 输入：仅一行的合法 MOT detection 文件。
- 预期：程序完成单帧处理并生成输出文件。
- 原始实际：`np.loadtxt` 返回一维数组，`seq_dets[:,0]` 抛出 `IndexError`。
- 根因：未统一最小规模检测文件的数组维度。
- 最小修复：`np.atleast_2d(np.loadtxt(...))`。
- 状态：目标测试与阶段一全量回归均通过。

## 测试边界

阶段一只覆盖正常输入、典型边界和常见跟踪场景。系统性非法输入、NaN/Inf、性质测试、随机生成和长序列稳定性留给阶段二 AI 增强测试。
